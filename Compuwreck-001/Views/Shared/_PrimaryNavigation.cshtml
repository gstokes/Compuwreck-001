@using Microsoft.AspNet.Identity
<div class="header">
    <div class="row">
        <div id="navAdmin">
            @if (Request.IsAuthenticated) {
                <ul class="right">
                    @*<li class="has-dropdown">
                    <a href="#"><img src="~/Content/Themes/Default/images/icon_user.png" width="30" height="20" />@User.Identity.Name logged In</a>
                    <ul class="dropdown">*@
                    <li>@Html.ActionLink("Logout", "LogOff", "Account")</li>
                    <li>@Html.ActionLink("Profile", "Edit", "Account", new { id = @User.Identity.Name }, null)</li>
                    @if (@User.IsInRole("Admin")) {
                        <li>@Html.ActionLink("Account Admin", "Index", "Account")</li>
                        <li>@Html.ActionLink("Add Shipwreck", "Create", "Shipwreck")</li>
                    }@*</ul>
                    </li>*@
                </ul>
            }
            else {
                <ul class="right">
                    <li>@Html.ActionLink("LOGIN", "Login", "Account")</li>
                    <li>@Html.ActionLink("REGISTER", "Register", "Account")</li>
                </ul>
            }
        </div>


        <div class="headerLogo">
            <img src="~/Content/images/headerLogo.png"/>
        </div>

        <nav class="nav-collapse">
            <ul>
                <li>@Html.ActionLink("HOME", "Index", "Shipwreck")</li>
                <li>@Html.ActionLink("SEARCH", "Index", "Shipwreck")</li>
                <li>@Html.ActionLink("ABOUT", "Index", "Shipwreck")</li>
                <li>@Html.ActionLink("CONTACT", "Index", "Shipwreck")</li>
            </ul>
        </nav>
    </div>
</div>
